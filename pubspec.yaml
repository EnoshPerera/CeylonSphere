name: gggg
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  google_maps_webservice: ^0.0.20-nullsafety.5
  google_maps_flutter: ^2.10.1
  geolocator: ^11.0.0
  http: ^0.13.6
  permission_handler: ^10.0.0
  flutter_polyline_points: ^1.0.0
  provider: ^6.0.0
  firebase_core: ^2.15.1 # Updated to the latest version
  firebase_auth: ^4.11.1 # Updated to the latest version
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.1
  url_launcher: ^6.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/facebook-logo.png
    - assets/firstscreen.jpg
    - assets/google-logo.png
    - assets/logo.png
    - assets/Logo-10.png
    - assets/Logo-12.png
    - assets/logo-14.png
    - assets/toyota-corolla.jpg
    - assets/Honda-Civic.jpeg
    - assets/Suzuki-Swift.jpg

  uses-material-design: true